(()=>{
  const $=s=>document.querySelector(s),esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c])),pretty=v=>String(v||'').replaceAll('_',' ').toLowerCase().replace(/\b\w/g,c=>c.toUpperCase());
  let data=null;
  const uniq=a=>[...new Set(a.filter(Boolean))].sort();
  function option(v){return `<option value="${esc(v)}">${esc(pretty(v))}</option>`}
  function render(){const f=$('#forceFilter').value,t=$('#traditionFilter').value,rows=(data?.events||[]).filter(e=>(f==='ALL'||(e.force_types||[]).includes(f))&&(t==='ALL'||(e.tradition_scope||[]).includes(t)));$('#historyCount').textContent=`${rows.length} historical forces in the current research preview`;$('#historyList').innerHTML=rows.map(e=>`<article class="history-event"><header><div><div class="noema-kicker">${esc((e.force_types||[]).map(pretty).join(' · '))}</div><h3>${esc(e.label)}</h3><div class="small">${esc((e.regions||[]).join(' · '))}</div></div><div class="period">${esc(e.period)}</div></header><p>${esc(e.statement)}</p><div class="impact"><b>Mechanisms to test</b><div class="state-legend">${(e.mechanisms_to_test||[]).map(x=>`<span data-state="interpreted">${esc(x)}</span>`).join('')}</div><div class="small"><b>Affected scope:</b> ${esc((e.tradition_scope||[]).join(' · '))}</div><div class="sources">${(e.source_links||[]).map(s=>`<a href="${esc(s.url)}" target="_blank" rel="noopener noreferrer">${esc(s.title)} · ${esc(s.institution)} ↗</a>`).join('')}</div></div></article>`).join('')||'<div class="empty">No historical-force records match those filters.</div>'}
  async function boot(){try{const r=await fetch('./historical-forces-v18.json');if(!r.ok)throw new Error(`HTTP ${r.status}`);data=await r.json();const forces=uniq((data.events||[]).flatMap(e=>e.force_types||[])),traditions=uniq((data.events||[]).flatMap(e=>e.tradition_scope||[]));$('#forceFilter').innerHTML='<option value="ALL">All historical forces</option>'+forces.map(option).join('');$('#traditionFilter').innerHTML='<option value="ALL">All traditions</option>'+traditions.map(v=>`<option value="${esc(v)}">${esc(v)}</option>`).join('');$('#forceFilter').addEventListener('change',render);$('#traditionFilter').addEventListener('change',render);render()}catch(e){$('#historyCount').textContent='Historical layer unavailable';$('#historyList').innerHTML=`<div class="notice">Could not load historical forces: ${esc(e.message)}</div>`}}
  boot();
})();
