(() => {const $=s=>document.querySelector(s),esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c])),pretty=v=>String(v||'').replaceAll('_',' ').toLowerCase().replace(/\b\w/g,c=>c.toUpperCase());const get=async u=>{try{const r=await fetch(u,{cache:'no-store'});return r.ok?await r.json():null}catch{return null}};const patterns=d=>(d?.cohorts||[]).flatMap(c=>(c.candidates||[]).map(x=>({...x,source:c.source}))).sort((a,b)=>(a.q_bh??1)-(b.q_bh??1)||Math.abs(b.phi||0)-Math.abs(a.phi||0));async function boot(){const [fed,media,pat]=await Promise.all([get('./religion-federation.json'),get('./museum-reference-media.json'),get('./pattern-candidates.json')]);const s=fed?.summary||{};$('#homeState').textContent=`${Number(s.comparable_belief_system_profiles||0).toLocaleString()} comparable profiles · ${Number(s.accepted_semantic_assertions||0).toLocaleString()} accepted semantic assertions · research preview`;const imgs=(media?.records||[]).filter(x=>x.display_gate==='REFERENCE_RENDER_ALLOWED_PROVIDER_ASSERTED_IDENTITY').slice(0,3);$('#homeVisual').innerHTML=imgs.length?imgs.map(r=>`<figure><img src="${esc(r.image_url||r.thumbnail_url)}" alt="${esc(r.provider_title||r.entity_name)}" referrerpolicy="no-referrer"><figcaption><b>${esc(r.entity_name)}</b><br>${esc(r.provider_title)} · ${esc(r.date_display||'undated')}<br>${esc(r.rights)} · museum reference, not evidence</figcaption></figure>`).join(''):`<figure><div class="fallback">◉</div><figcaption>Open-access historical reference media will appear here when available.</figcaption></figure>`;const rows=patterns(pat).slice(0,4);$('#homePatterns').innerHTML=rows.length?rows.map(x=>`<a class="home-v3-pattern" href="patterns.html"><div><b>${esc(pretty(x.facet_a))} ↔ ${esc(pretty(x.facet_b))}</b><span>${esc(x.source)} · n=${Number(x.n_comparable||0).toLocaleString()} · descriptive candidate</span></div><em>φ ${Number(x.phi||0).toFixed(2)}</em></a>`).join(''):'<div class="empty">No defensible pattern candidates under the current screen.</div>';$('#homeSearch').addEventListener('submit',e=>{e.preventDefault();const q=$('#homeQ').value.trim();location.href=`explore.html${q?`?q=${encodeURIComponent(q)}`:''}`})}boot()})();